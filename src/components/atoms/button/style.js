import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    buttonContainer: {
        padding: 13,
        width: 200,
        borderRadius: 30
    },
    textContainer: {
        color: 'white',
        textAlign: 'center',
        fontSize: 15,
        fontWeight: 'bold',
        textTransform: 'uppercase'
    }
})